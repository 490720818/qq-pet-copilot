.venv/
__pycache__/
*.pyc
.env
screenshots
# 打包产物（spec 是 build.py 的输入，需入库，不再忽略）
build/
dist/
# 运行状态（进度、日志、调试图）
runs/
# 控件树抓页产物（tools/dump_hierarchy.py）
xml/
# 个人配置（模板为 config.example.yaml）
config.yaml

# 第三方二进制（scrcpy），用 tools/fetch_scrcpy.py 拉取
scrcpy-win64/
